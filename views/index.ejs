<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Danh sách File từ Drive</title>
  <style>
    body { font-family: Arial, sans-serif; margin: 20px; background-color: #f4f4f4; }
    h1 { color: #333; }
    ul { list-style-type: none; padding: 0; }
    li {
      background-color: #fff;
      margin-bottom: 10px;
      padding: 15px;
      border: 1px solid #ddd;
      border-radius: 5px;
      display: flex;
      align-items: center;
    }
    .file-name { 
      font-size: 1.1em; 
      color: #0056b3;
      white-space: nowrap;
      overflow: hidden;
      text-overflow: ellipsis;
    }
    
    /* CSS cho nút Tải xuống (.download-btn) ĐÃ BỊ XÓA */

    /* CSS cho link Thư mục */
    .folder-link {
      text-decoration: none;
      color: #0056b3;
      font-weight: bold;
    }
    .folder-link:hover { text-decoration: underline; }

    /* THÊM MỚI: CSS cho link File (thay cho nút tải) */
    .file-link {
      text-decoration: none;
      color: #0056b3;
    }
    .file-link:hover { text-decoration: underline; }

    /* CSS cho link Quay về */
    .nav-link {
      display: inline-block;
      margin-bottom: 20px;
      font-size: 1.1em;
      text-decoration: none;
      padding: 8px 15px;
      background-color: #007bff;
      color: white;
      border-radius: 5px;
    }
    .nav-link:hover { background-color: #0056b3; }
  </style>
</head>
<body>
  <h1>Danh sách File của tôi</h1>

  <% if (currentFolderId !== rootFolderId) { %>
    <a href="/" class="nav-link">⬅️ Quay về thư mục gốc</a>
  <% } %>


  <% if (items && items.length > 0) { %>
    <ul>
      <% items.forEach(item => { %>
        <li>
          
          <% if (item.mimeType === 'application/vnd.google-apps.folder') { %>
            <span class="file-name">
              <a href="/<%= item.id %>" class="folder-link">
                📁 <%= item.name %>
              </a>
            </span>

          <% } else { %>
            <span class="file-name">
              <a href="https://drive.google.com/uc?export=download&id=<%= item.id %>" 
                 class="file-link" 
                 target="_blank">
                <%= item.name %>
              </a>
            </span>
            <% } %>

        </li>
      <% }); %>
    </ul>
  <% } else { %>
    <p>Không tìm thấy file nào trong thư mục.</p>
  <% } %>

</body>
</html>
